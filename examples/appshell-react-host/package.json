{
  "name": "appshell-react-host",
  "version": "0.0.1",
  "description": "Full example using @appshell/manifest-webpack-plugin, @appshell/cli and @appshell/react-host to build Module Federation micro-frontends",
  "main": "index.js",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "repository": "https://github.com/navaris/appshell.git",
  "author": "Robert Hamilton <rh@navaris.com>",
  "scripts": {
    "bootstrap": "npx lerna bootstrap",
    "build": "dotenv -- lerna run --scope=react-appshell-host-components build && dotenv -- lerna run --ignore=react-appshell-host-components build",
    "postbuild": "dotenv -- yarn generate:manifest",
    "generate:manifest": "appshell generate manifest --configsDir appshell_configs --outDir apps/sample-mfe-container",
    "clean": "lerna run --parallel clean && rm -rf coverage appshell_configs appshell.manifest.json",
    "lint": "eslint apps/**/*.ts",
    "serve": "dotenv -- lerna run --parallel serve",
    "prestart": "dotenv -- yarn generate:manifest",
    "start": "dotenv -- run-p start:apps start:host",
    "start:apps": "lerna run --parallel start",
    "start:host": "docker compose up",
    "stats": "lerna run --parallel stats",
    "test": "jest",
    "test:ci": "yarn test a --watchAll=false --coverage --reporters=jest-junit"
  },
  "dependencies": {
    "@appshell/cli": "0.1.5",
    "@appshell/manifest-webpack-plugin": "0.1.5",
    "@testing-library/jest-dom": "5.16.5",
    "@testing-library/react": "13.4.0",
    "@testing-library/user-event": "13.5.0",
    "@types/jest": "27.5.2",
    "@types/node": "16.18.12",
    "@types/react": "18.0.28",
    "@types/react-dom": "18.0.11",
    "craco": "0.0.3",
    "npm-run-all": "4.1.5",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "react-refresh": "0.14.0",
    "react-scripts": "5.0.1",
    "react-spinners": "0.13.8",
    "styled-components": "5.3.8",
    "tsconfig-paths-webpack-plugin": "4.0.0",
    "typescript": "4.9.5",
    "web-vitals": "2.1.4",
    "webpack": "5.75.0",
    "webpack-cli": "5.0.1",
    "webpack-dev-server": "4.11.1"
  },
  "resolutions": {
    "react-refresh": "0.14.0"
  },
  "overrides": {
    "react-scripts": {
      "react-refresh": "0.14.0"
    }
  },
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@pmmmwh/react-refresh-webpack-plugin": "0.5.10",
    "@types/styled-components": "5.1.26",
    "webpack-visualizer-plugin2": "1.0.0"
  }
}